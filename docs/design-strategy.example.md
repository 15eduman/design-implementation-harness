# Design Strategy Example

Use this file to describe how the harness should relate to your broader design
system.

Recommended split:

```txt
brand/style guide     = visual direction
module spec           = source-derived implementation rules
component inventory   = reusable source-backed components
QA checklist          = acceptance criteria
```

When sources conflict, prefer source components and screenshots over generic
style guidance.

